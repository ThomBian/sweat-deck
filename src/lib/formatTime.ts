/** MM:SS for workout countdowns and prescription labels (tabular, glanceable). */
export function formatMSS(totalSec: number): string {
  const m = Math.floor(totalSec / 60);
  const s = totalSec % 60;
  return `${m}:${s.toString().padStart(2, '0')}`;
}
